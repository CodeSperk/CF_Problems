#include <stdio.h>
#include <limits.h>
#include <stdbool.h>

int main()
{
    int n;
    scanf("%d", &n);

    int arr[n];

    for(int i = 0; i<n; i++){
        scanf("%d", &arr[i]);
    }

    int reversed[n];
    for(int j = 0; j < n; j++){
        for(int k = n -1; k >= 0; k--){
            reversed[j]
        }
    }  
    
    bool isSame = true;

    for(int s = 0; s < n; s ++){
        if(arr[s])
    }


    return 0;
}